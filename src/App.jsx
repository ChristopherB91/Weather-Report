import "./App.css";

function App() {
  return (
    <div
      style={{
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
      }}
    >
      <input placeholder = 'Enter city'style={{}}/>
      <button>Search</button>
    </div>
  );
}

export default App;
